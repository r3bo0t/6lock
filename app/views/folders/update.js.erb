<% if @folder.valid? %>
  $(function() {
    $('#link_<%= @folder.id %>').next('.rename_element').hide()
    $('#link_<%= @folder.id %>').html('<%= @folder.name %>').show()
  })
<% else %>
  alert("The name you submitted contains invalid characters or is too long (> 23).")
<% end %>